# To check wether python is installed or not
python --version

# To create a virtual environment named venv
python -m venv venv

# To activate the virtual environment
venv\Scripts\activate

# To check which python version is being used in priority
where python

# To upgrade PIP package installer
python -m pip install --upgrade pip

# To install fastapi and uvicorn
python -m pip install scikit-learn

#Lists the pacakges with versions like package.json in node
python -m pip freeze > requirements.txt

# To view the project requirements
type requirements.txt

# Install from requirements
pip install -r requirements.txt

#Lists the pacakges with versions like package.json in node
python -m pip freeze > requirements.txt

# To run the app/scripts
python main.py

# To deactivate the virtual environment
deactivate